import type { Types } from '@cornerstonejs/core';
import { BaseTool } from '../base';
import { PublicToolProps, ToolProps, EventTypes, Segmentation } from '../../types';
declare class SegmentSelectTool extends BaseTool {
    static toolName: any;
    private hoverTimer;
    constructor(toolProps?: PublicToolProps, defaultToolProps?: ToolProps);
    mouseMoveCallback: (evt: EventTypes.InteractionEventType) => boolean;
    onSetToolEnabled: () => void;
    onSetToolActive: () => void;
    onSetToolDisabled: () => void;
    _setActiveSegment(evt?: EventTypes.InteractionEventType): void;
    _setActiveSegmentLabelmap(activeSegmentation: Segmentation, worldPoint: Types.Point3, viewport: Types.IStackViewport | Types.IVolumeViewport): void;
}
export default SegmentSelectTool;
